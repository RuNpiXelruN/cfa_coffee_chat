<%= form_tag user_list_path, method: 'get', remote: true do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<div id="user-items-grid">
  <%= render partial: 'users/user_list', users: @users %>
</div>
