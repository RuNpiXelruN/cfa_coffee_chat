<%= form_tag chat_list_path, method: 'GET', remote: true do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<div id="items-grid">
  <%= render partial: 'chats/chat_list', locals: {chats: @chats} %>
</div>
